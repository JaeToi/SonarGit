pipeline {
    agent any

    stages {
        stage('build'){
            steps{
                build wait: false, job: 'GitHub_Project', waitForStart: true
            }
        }
        stage ('checkout'){
            steps {
                checkout scmGit(branches: [[name: '*/main']], extensions: [], userRemoteConfigs: [[credentialsId: 'GitHub_Token', url: 'https://github.com/JaeToi/SonarGit.git']])
            }
        }
  

          stage('SCM') {
            steps {
                   git branch: "main",
                   url: "https://github.com/foo/bar.git"
            }
          }
          stage('SonarQube Analysis') {
            steps {
           
                withSonarQubeEnv('SonarQube_Server') {
                  sh "C:\sonarqube-10.5.1.90531\sonar-scanner-cli-5.0.1.3006-windows\sonar-scanner-5.0.1.3006-windows\bin\sonar-scanner"
                }
            }
          }
  }
}
